import { IListSort } from '@base/list/models/list-sort.model';

export const DEFAULT_WIDTH: number = 160;
export const ZERO_COLUMN_WIDTH: string = '30px';
export const LIST_SORT: IListSort = { sortBy: 'id', sortDirection: 'ascend', serverSide: false};
